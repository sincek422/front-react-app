{
  "name": "front-react-app",
  "version": "0.1.0",
  "homepage": "https://sincek422.github.io/front-react-app",
  "private": true,
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build"
  },
  "proxy": "https://www.tabechao.com",
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
